import { useEffect, useState } from 'react'
import { X } from 'lucide-react'
import type { Category, Transaction } from '../types'
import { Button } from './ui'
type Props={categories:Category[]; transaction?:Transaction; onClose:()=>void; onSave:(data:object)=>Promise<void>}
export function TransactionModal({categories,transaction,onClose,onSave}:Props){const [saving,setSaving]=useState(false);const [form,setForm]=useState({description:transaction?.description||'',amount:transaction?.amount||'',type:transaction?.type||'EXPENSE',category_id:String(transaction?.category_id||categories[0]?.id||''),date:transaction?.date||new Date().toISOString().slice(0,10),notes:transaction?.notes||''}); useEffect(()=>{const close=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();addEventListener('keydown',close);return()=>removeEventListener('keydown',close)},[onClose]);const submit=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);try{await onSave({...form,amount:Number(form.amount),category_id:Number(form.category_id)})}finally{setSaving(false)}};return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" role="dialog" aria-modal="true" aria-label="Transação"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl border border-border bg-panel p-6 shadow-2xl"><div className="mb-6 flex items-center justify-between"><h2 className="text-lg font-bold">{transaction?'Editar':'Adicionar'} transação</h2><button type="button" onClick={onClose} aria-label="Fechar"><X/></button></div><div className="grid gap-4 sm:grid-cols-2"><label>Tipo<select value={form.type} onChange={e=>setForm({...form,type:e.target.value as 'INCOME'|'EXPENSE'})}><option value="EXPENSE">Despesa</option><option value="INCOME">Receita</option></select></label><label>Categoria<select required value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})}>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="sm:col-span-2">Descrição<input required value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Valor<input required min="0.01" step="0.01" type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></label><label>Data<input required type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label className="sm:col-span-2">Observação<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label></div><div className="mt-6 flex justify-end gap-3"><Button type="button" className="bg-transparent text-muted hover:bg-card" onClick={onClose}>Cancelar</Button><Button disabled={saving}>{saving?'Salvando...':'Salvar transação'}</Button></div></form></div>}
