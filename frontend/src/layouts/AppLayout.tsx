import { BarChart3, LayoutDashboard, Menu, ReceiptText, Tags, WalletCards, X } from 'lucide-react'
import { NavLink, Outlet, useLocation } from 'react-router-dom'
import { useState } from 'react'
const links=[['/',LayoutDashboard,'Dashboard'],['/transactions',ReceiptText,'Transações'],['/categories',Tags,'Categorias'],['/budget',WalletCards,'Orçamento'],['/reports',BarChart3,'Relatórios']] as const
export function AppLayout(){const [open,setOpen]=useState(false);const location=useLocation();const current=links.find(l=>l[0]===location.pathname)?.[2]||'FinTrack';return <div className="min-h-screen bg-base"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-border bg-panel p-5 transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="mb-10 flex items-center justify-between"><span className="text-xl font-black tracking-[0.16em] text-neon">FINTRACK</span><button className="lg:hidden" onClick={()=>setOpen(false)} aria-label="Fechar menu"><X/></button></div><nav className="space-y-2">{links.map(([href,Icon,label])=><NavLink end={href==='/'} key={href} to={href} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium transition ${isActive?'bg-neon text-base':'text-muted hover:bg-card hover:text-white'}`}><Icon size={19}/>{label}</NavLink>)}</nav><p className="absolute bottom-6 text-xs text-subtle">Controle financeiro pessoal</p></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 flex items-center gap-4 border-b border-border bg-base/90 px-5 py-4 backdrop-blur lg:px-8"><button className="lg:hidden" onClick={()=>setOpen(true)} aria-label="Abrir menu"><Menu/></button><div><h1 className="font-bold">{current}</h1><p className="text-sm text-muted">Organize sua vida financeira.</p></div></header><main className="p-5 lg:p-8"><Outlet/></main></div>{open&&<button className="fixed inset-0 z-30 bg-black/60 lg:hidden" aria-label="Fechar menu" onClick={()=>setOpen(false)}/>}</div>}
